# BattleBox environment configuration

# HTTP + Socket.IO port
PORT=3000

# Exact browser origin allowed to connect over Socket.IO
ALLOWED_ORIGIN=http://localhost:3000

# Set true when running behind a trusted reverse proxy/load balancer
TRUST_PROXY=false

# Runtime environment
aNODE_ENV=development
